#include "holberton.h"

char *_strncpy(char *dest, char *src, int n)
{
	int h, i;
	for (h = 0; src[i] != '\0'; h++)
	{
	}
	for (i = 0; i < n && src[i] != '\0'; i++)
	{
		dest[] = src[];
	}
	return (dest);
}
